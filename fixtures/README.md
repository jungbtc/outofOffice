# Fixtures

`korean-english.csv` is the initial UTF-8 text fixture. Binary DOCX, PPTX, XLSX, ODT, ODP, ODS, and versioned internal-package fixtures will be added with the corresponding import/export work; they are intentionally absent rather than fake placeholders.
